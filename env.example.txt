PORT=3001
NODE_ENV=development

# MongoDB Atlas connection string (replace with your own)
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/aizus_kitchen?retryWrites=true&w=majority

# Long random secret (>= 16 chars)
JWT_SECRET=change_this_to_a_long_random_secret

# Your React dev server origin (Vite default)
CLIENT_ORIGIN=http://localhost:5173

# Admin bootstrap (used by: npm run seed:admin)
ADMIN_BOOTSTRAP_USER=admin
ADMIN_BOOTSTRAP_PASS=change_this_password


